{
  "name": "club-members",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.1"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev -r --test-scheduled src/index.ts",
    "deploy": "wrangler publish src/index.ts",
    "nar:fetch": "npx tsx src/publish_nar_certs.ts"
  }
}
