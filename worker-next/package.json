{
  "name": "@flightcard/worker-next",
  "version": "0.0.0",
  "type": "module",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "@types/node": "^22.10.3",
    "puppeteer": "^23.11.1",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "private": true,
  "scripts": {
    "cf-codegen": "wrangler types",
    "db:do": "wrangler d1 execute -y flightcard --command",
    "db:migrate:reset": "npm run db:do \"drop table migrations\"",
    "db:migrate": "wrangler d1 migrations apply flightcard --local",
    "db:schema": "npm run db:do \"SELECT name, sql FROM sqlite_schema\"",
    "deploy": "wrangler deploy && npm run db:migrate --remote",
    "start": "wrangler dev --test-scheduled src/index.ts",
    "test": "npx -y tsx --test src/**/*.test.ts",
    "test:watch": "npx -y tsx --test --watch src/**/*.test.ts"
  },
  "dependencies": {
    "@flightcard/common": "*",
    "@flightcard/db": "*",
    "@flightcard/models": "*",
    "cookie": "^1.0.2"
  }
}
